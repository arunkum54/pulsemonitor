{
  "name": "uptime-monitor-backend",
  "version": "1.0.0",
  "description": "Uptime monitor API: registers URLs, pings them on a schedule, stores health check history.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-cron": "^3.0.3"
  }
}
